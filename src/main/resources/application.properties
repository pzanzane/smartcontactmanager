spring.application.name=scm2.0
server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/scm20
spring.datasource.username=root

# this password needs to match with password when you installed mysql server.
spring.datasource.password=Clarity4u!

#jpa configuration
# This will log all the SQL statements to console, executed by hibernate.
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.properties.hibernate.format_sql=true